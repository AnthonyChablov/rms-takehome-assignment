import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';

describe('earthquakesApi.tsx', () => {
  it('should render in the DOM', () => {
    // Arrange
    // Act
    // Assert
  });
});
